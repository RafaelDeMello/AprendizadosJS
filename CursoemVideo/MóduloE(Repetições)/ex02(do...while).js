/*
var c = 1
while (c <= 3){
    console.log(`Passo ${c}`)
    c++
}
*/
var c = 1   //declara variavel contador
do { // Executa o bloco de código ao menos uma vez
    console.log(`Passo ${c}`) // Bloco de códiugo
    c++ //Bloco de código
} while (c <= 3)  // Verifica a condição, caso seja verdadeira volta para a execução do bloco de código